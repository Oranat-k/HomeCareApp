<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="akkaudom.oranat.th.ac.su.reg.homecarese">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    // ขออนุญาตดึงรูปภาพ
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity
            android:name=".LoginActivity"
            android:screenOrientation="portrait">
            //จอเเนวตั้ง



        </activity>
        <activity
            android:name=".AddPlannerActivity"
            android:parentActivityName=".HistoryMedicineActivity" />
        <activity
            android:name=".PlannerActivity"
            android:parentActivityName=".AddPlannerActivity">

        </activity>
        <activity
            android:name=".HistoryMedicineActivity"
            android:parentActivityName=".PlannerActivity" />
        <activity
            android:name=".AddTherapyActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".AddSymptomActivity" />
        <activity android:name=".AddSugarActivity" />
        <activity android:name=".AddPressureActivity" />
        <activity android:name=".AddDoctorActivity" />
        <activity android:name=".PlannerListActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".ProfileActivity" />
        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />
        <activity android:name=".HomeActivity"></activity>
    </application>

</manifest>